version: "3.9"

services:
  redis:
    image: redis:alpine
    ports:
      - "6379:6379"

  brain-core:
    build: ./brain-core
    ports:
      - "8000:8000"
    depends_on:
      - redis
    env_file:
      - .env
